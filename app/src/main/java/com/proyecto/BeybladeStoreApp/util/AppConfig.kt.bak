package com.proyecto.BeybladeStoreApp.util

object AppConfig {
    // Toggle DataStore usage to isolate crashes during debugging.
    // Set to true to enable full DataStore-backed persistence.
    const val USE_DATASTORE = false
}
